import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  typography: {
    fontFamily: [
      'Montserrat'
    ],
    fontStyle: 'normal',
    fontDisplay: 'swap',
    fontWeight: 400
  }
})

export default theme
